{
  "name": "movie-theater-frontend",
  "version": "1.0.0",
  "description": "Frontend for Movie Theater Project",
  "scripts": {
    "start": "live-server --port=56719 --host=127.0.0.1 --open=HTML/home.html --entry-file=HTML/home.html",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "bootstrap-icons": "^1.11.3"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
